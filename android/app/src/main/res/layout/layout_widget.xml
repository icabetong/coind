<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widgetContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_container_padding"
    android:background="@drawable/widget_background">

    <TextView
        android:id="@+id/symbolTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@id/triggerRefreshButton"
        android:textColor="@android:color/white"/>

    <ImageButton
        android:id="@+id/triggerRefreshButton"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_alignParentRight="true"
        android:background="@android:color/transparent"
        android:src="@drawable/outline_refresh_24"/>

    <TextView
        android:id="@+id/valueTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/symbolTextView"
        android:textSize="20sp"
        android:textColor="@color/color_secondary"
        android:textStyle="bold"/>

    <TextView
        android:id="@+id/lastUpdatedTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/valueTextView"
        android:textSize="12sp"
        android:textColor="@color/color_text_secondary"/>

</RelativeLayout>